# Cloudflare Web Analytics beacon token (cookieless, no consent banner).
# Get it: Cloudflare dash -> Analytics & Logs -> Web Analytics -> add
# chasqui.chat -> copy the "token" value from the snippet.
# Public client-side id (safe to expose); leave unset in dev to skip analytics.
PUBLIC_CF_BEACON_TOKEN=
